data_root: datasets/M3FD_Detection
split: train
image_size: [256, 320]
num_classes: 6
batch_size: 2
epochs: 20
lr: 0.0001
detector_backend: yolo_like
use_sam: true
use_feedback: true
fusion_weight: 1.0
detection_weight: 1.0
